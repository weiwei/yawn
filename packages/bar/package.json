{
  "name": "@yawn/bar",
  "packageManager": "yarn@3.2.1",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "run -T jest",
    "bundle": "run -T webpack --config webpack.config.js",
    "build": "run -T tsc --build --verbose",
    "clean": "run -T tsc --build --clean"
  },
  "dependencies": {
    "@yawn/foo": "workspace:*"
  }
}
